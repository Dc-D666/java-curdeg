# 个人中心页面优化 - 实现计划

## [x] Task 1：创建专门的CSS样式文件
- **优先级**：P0
- **依赖**：None
- **描述**：
  - 在 `java-fx/src/main/resources/com/teach/javafx/css/` 目录下创建 `personal-center.css`
  - 定义个人中心相关的所有样式类，包括：
    - 导航按钮样式（默认、悬停、选中状态）
    - 卡片样式（阴影、圆角、背景色）
    - 头像圆形样式
    - 表单样式（标签、输入框、下拉框）
    - 统计卡片样式
    - 按钮样式（编辑、保存、取消）
- **验收标准**：FR-6
- **测试要求**：
  - `human-judgment`：CSS文件结构清晰，样式类命名规范
- **备注**：不需要新增数据库表或字段

## [x] Task 2：优化个人中心左侧导航栏
- **优先级**：P0
- **依赖**：Task 1
- **描述**：
  - 修改 `personal-center.fxml`
  - 应用新创建的CSS样式
  - 改进导航按钮的视觉效果（选中状态、悬停效果）
  - 保持原有的控制器绑定不变
- **验收标准**：FR-1, AC-1
- **测试要求**：
  - `human-judgment`：导航栏样式美观，有清晰的状态反馈
  - `programmatic`：点击导航按钮能正常切换页面

## [x] Task 3：重新设计个人资料页面为卡片式布局
- **优先级**：P0
- **依赖**：Task 1
- **描述**：
  - 重构 `personal-profile.fxml`
  - 页面分为三个主要卡片：
    1. 基本信息卡片（头像、学号、昵称、个性签名）
    2. 详细资料卡片（姓名、学院、性别、生日、邮箱、电话、地址、个人简介及对应隐私设置）
    3. 统计信息卡片（发帖数、关注数、粉丝数）
  - 保持所有原有的fx:id和控制器绑定
  - 应用卡片式CSS样式
- **验收标准**：FR-2, AC-2
- **测试要求**：
  - `human-judgment`：页面布局整洁，卡片分区清晰
  - `programmatic`：所有数据字段正常显示

## [x] Task 4：实现头像圆形显示效果
- **优先级**：P0
- **依赖**：Task 1, Task 3
- **描述**：
  - 在CSS中定义头像的圆形样式
  - 在FXML中应用样式类
  - 修改 `PersonalProfileController.java`，添加必要的代码确保头像正确裁剪为圆形（使用Clip）
- **验收标准**：FR-3, AC-3
- **测试要求**：
  - `human-judgment`：头像完美显示为圆形，边缘平滑
  - `programmatic`：上传新头像后仍保持圆形显示

## [x] Task 5：统一表单对齐和样式
- **优先级**：P0
- **依赖**：Task 1, Task 3
- **描述**：
  - 在CSS中定义统一的表单样式
  - 确保所有标签右对齐，输入框左对齐
  - 统一间距和边框样式
  - 优化隐私设置下拉框的样式
- **验收标准**：FR-4, AC-4
- **测试要求**：
  - `human-judgment`：表单对齐整齐，样式统一
  - `programmatic`：编辑和保存功能正常

## [x] Task 6：优化统计卡片视觉效果和交互
- **优先级**：P1
- **依赖**：Task 1, Task 3
- **描述**：
  - 在CSS中定义统计卡片的美观样式
  - 添加悬停效果（颜色变化、阴影加深）
  - 保持原有的点击跳转功能
- **验收标准**：FR-5, AC-5
- **测试要求**：
  - `human-judgment`：统计卡片视觉效果好，悬停反馈清晰
  - `programmatic`：点击卡片能正常跳转

## [x] Task 7：功能完整性测试
- **优先级**：P0
- **依赖**：Task 2, Task 3, Task 4, Task 5, Task 6
- **描述**：
  - 全面测试个人中心的所有功能
  - 确保编辑、保存、取消功能正常
  - 确保头像上传功能正常
  - 确保隐私设置功能正常
  - 确保所有数据正确显示和保存
- **验收标准**：NFR-1, AC-6
- **测试要求**：
  - `programmatic`：所有功能与优化前一致，无功能回归
  - `human-judgment`：整体用户体验良好
