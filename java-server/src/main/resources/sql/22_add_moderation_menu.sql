-- ===============================================================
-- 添加内容审核管理菜单
-- 功能：在系统管理菜单下添加内容审核菜单项
-- 用户类型：1-super, 2-admin
-- ===============================================================

USE java_2_48;

-- 添加内容审核管理菜单项 (id=505, pid=500)
INSERT IGNORE INTO menu (id, pid, name, title, user_type_ids) 
VALUES (505, 500, 'admin-moderation', '内容审核', '1,2');

-- 验证菜单数据
SELECT id, pid, name, title, user_type_ids FROM menu WHERE pid = 500 ORDER BY id ASC;

-- ===============================================================
-- 完成！
-- ===============================================================
