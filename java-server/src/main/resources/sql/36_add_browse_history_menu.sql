-- ===============================================================
-- 浏览历史菜单项添加脚本
-- 执行前请确保已备份数据库！
-- ===============================================================

-- 使用数据库（请根据实际情况修改数据库名）
USE java_2_48;

-- ===============================================================
-- 添加浏览历史菜单项
-- ===============================================================

-- 二级菜单：浏览历史 (pid=300，属于个人中心)
INSERT IGNORE INTO menu (id, pid, name, title, user_type_ids) VALUES (309, 300, 'browse-history', '浏览历史', '1,2,3');

-- ===============================================================
-- 验证
-- ===============================================================

-- 查看个人中心的子菜单
SELECT * FROM menu WHERE pid = 300 ORDER BY id ASC;
