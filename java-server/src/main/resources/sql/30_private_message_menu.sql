-- ===============================================================
-- 私信功能菜单配置脚本
-- 功能：在个人中心添加"我的私信"菜单项
-- 使用前请确保已备份数据库！
-- ===============================================================

-- 使用数据库（请根据实际情况修改数据库名）
USE java_2_48;

-- 添加"我的私信"菜单项到个人中心（id=309）
INSERT IGNORE INTO menu (id, user_type_ids, pid, name, title)
VALUES (309, '1,2,3', 300, 'MyMessages', '我的私信');

-- 验证菜单配置
SELECT * FROM menu WHERE pid = 300 ORDER BY id;
