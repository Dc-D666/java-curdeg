-- ===============================================================
-- 评论图片功能数据库表设计脚本
-- 执行前请确保已备份数据库！
-- ===============================================================

USE java_2_48;

-- ===============================================================
-- 扩展 bbs_comment 表（添加图片字段）
-- ===============================================================

-- 方法1：直接添加字段（如果字段不存在）
-- 注意：如果字段已存在，这行会报错，请注释掉后执行
ALTER TABLE bbs_comment 
ADD COLUMN image_urls TEXT COMMENT '评论图片URL（逗号分隔）' AFTER content;

-- 方法2：如果报错说字段已存在，请用下面这条查看表结构，确认字段是否已添加
-- DESC bbs_comment;

-- 添加索引（可选，用于优化查询）
-- ALTER TABLE bbs_comment ADD INDEX idx_image_urls (image_urls(100));

-- ===============================================================
-- 验证脚本
-- ===============================================================

-- 查看表结构
DESC bbs_comment;
