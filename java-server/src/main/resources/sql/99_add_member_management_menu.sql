-- ===============================================================
-- 添加成员管理菜单项
-- ===============================================================

USE java_2_48;

-- ===============================================================
-- 添加成员管理菜单项到系统管理菜单下
-- ===============================================================
INSERT INTO menu (id, pid, name, title, user_type_ids) 
VALUES (506, 500, 'member-management', '成员管理', '1,2')
ON DUPLICATE KEY UPDATE
    name = VALUES(name),
    title = VALUES(title),
    user_type_ids = VALUES(user_type_ids);

-- ===============================================================
-- 验证菜单数据
-- ===============================================================
SELECT * FROM menu WHERE pid = 500 ORDER BY id ASC;

-- ===============================================================
-- 完成！
-- ===============================================================
